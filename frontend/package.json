{
  "name": "subagent-guild-frontend",
  "version": "1.0.0",
  "description": "Subagent Guild - Frontend for GitHub Pages + Supabase deployment",
  "main": "src/js/app.js",
  "scripts": {
    "dev": "webpack serve --mode development",
    "build": "node generate-config.js && webpack --mode production",
    "build:config": "node generate-config.js",
    "deploy": "npm run build && gh-pages -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "claude",
    "subagents",
    "github-pages",
    "supabase"
  ],
  "author": "Subagent Guild",
  "license": "MIT",
  "devDependencies": {
    "webpack": "^5.88.0",
    "webpack-cli": "^5.1.0",
    "webpack-dev-server": "^4.15.0",
    "html-webpack-plugin": "^5.5.0",
    "css-loader": "^6.8.0",
    "style-loader": "^3.3.0",
    "file-loader": "^6.2.0",
    "gh-pages": "^6.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.38.0"
  }
}